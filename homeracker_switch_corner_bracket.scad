// HomeRacker switch corner bracket - catches one corner of the switch.
// Print TWO of these (for one rail) plus two of the mirrored version
// (for the other rail) - see _homeracker_switch_corner_bracket_lib.scad
// for the full design notes and _mirrored.scad for the other-side piece.
include <_homeracker_switch_corner_bracket_lib.scad>
render_bracket(mirror_piece = false);
